version: 2

project_name: suppyhq

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: suppyhq
    main: .
    binary: suppyhq
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.Version={{ .Version }}

archives:
  - id: default
    formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md
      # The skill lives once, at skills/. Every plugin manifest points at it,
      # so the archive needs it explicitly now that .claude-plugin/ no longer
      # carries a copy.
      - skills/**
      - plugin.json
      - mcp.json
      - claude-plugins/**
      - .claude-plugin/**
      - .cursor-plugin/**
      - .codex-plugin/**

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"
      - "Merge branch"
